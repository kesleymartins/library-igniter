{
  "name": "library-igniter",
  "version": "1.0.0",
  "description": "A simple library app styled with bootstrap",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "dev": "esbuild app/Javascript/application.js --bundle --outfile=public/static/assets/js/bundle.js --watch",
    "build": "esbuild app/Javascript/application.js --bundle --outfile=public/static/assets/js/bundle.js --minify"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "0.19.4"
  },
  "dependencies": {
    "axios": "^1.5.1",
    "stimulus": "^3.2.2"
  }
}
